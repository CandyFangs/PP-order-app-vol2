<div class="product_choice">
	<% @categories.each do |category| %>
		<p class="lead">
	    <%= link_to "#{category.name}", category_path(category), class: "btn btn-lg btn-secondary" %>
	  </p>
	  <% end %>
</div>